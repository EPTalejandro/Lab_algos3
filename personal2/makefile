KC = kotlinc
KFLAGS = -include-runtime
PROG = Sudoku.jar
SRC = Sudoku.kt
SC = runMain.sh

all:
	$(KC) $(SRC) $(KFLAGS) -d $(PROG)
	@echo "#!/usr/bin/bash\njava -jar $(PROG) \$$*" > $(SC)
	@chmod 755 $(SC)

.PHONY : clean 

clean : 
	rm -rf $(PROG)
	rm -rf $(SC)
